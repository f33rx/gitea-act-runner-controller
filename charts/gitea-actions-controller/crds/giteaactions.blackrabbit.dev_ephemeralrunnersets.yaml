---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: ephemeralrunnersets.giteaactions.blackrabbit.dev
spec:
  group: giteaactions.blackrabbit.dev
  names:
    kind: EphemeralRunnerSet
    listKind: EphemeralRunnerSetList
    plural: ephemeralrunnersets
    shortNames:
    - ersets
    singular: ephemeralrunnerset
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EphemeralRunnerSet is the Schema for the ephemeralrunnersets
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EphemeralRunnerSetSpec defines the desired state of an EphemeralRunnerSet.
            properties:
              maxRunners:
                description: MaxRunners is the maximum number of runners (hard ceiling).
                format: int32
                type: integer
              minRunners:
                description: MinRunners is the minimum number of runners (warm floor).
                format: int32
                type: integer
              patchID:
                description: PatchID is a monotonically increasing token for listener/controller
                  coordination.
                format: int64
                type: integer
              replicas:
                description: Replicas is the desired number of ephemeral runners.
                format: int32
                type: integer
            required:
            - patchID
            - replicas
            type: object
          status:
            description: EphemeralRunnerSetStatus defines the observed state of an
              EphemeralRunnerSet.
            properties:
              availableReplicas:
                description: AvailableReplicas is the number of available runners.
                format: int32
                type: integer
              lastReconcilePatchID:
                description: LastReconcilePatchID is the last patchID the controller
                  reconciled.
                format: int64
                type: integer
              readyReplicas:
                description: ReadyReplicas is the number of ready runners.
                format: int32
                type: integer
              targetSize:
                description: TargetSize is the scaling decision output (desired runner
                  count).
                format: int32
                type: integer
              targetSizeUpdatedAt:
                description: TargetSizeUpdatedAt is when TargetSize was last updated.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
