---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: gitearunnersets.giteaactions.blackrabbit.dev
spec:
  group: giteaactions.blackrabbit.dev
  names:
    kind: GiteaRunnerSet
    listKind: GiteaRunnerSetList
    plural: gitearunnersets
    shortNames:
    - grs
    - grsets
    singular: gitearunnerset
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GiteaRunnerSet is the Schema for the gitearunnersets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GiteaRunnerSetSpec defines the desired state of GiteaRunnerSet.
            properties:
              giteaConfigSecretRef:
                description: GiteaConfigSecretRef is a reference to the Secret containing
                  the Gitea operator credential (a SecretKeySelector).
                properties:
                  key:
                    description: The key of the secret to select from.
                    type: string
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  optional:
                    description: Specify whether the Secret or its key must be defined.
                    type: boolean
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
              giteaConfigUrl:
                description: GiteaConfigUrl is the URL of the Gitea instance.
                type: string
              labels:
                description: Labels are the runner labels (e.g., ubuntu-latest, dind).
                items:
                  type: string
                type: array
              maxRunners:
                description: MaxRunners is the maximum number of ephemeral runners
                  to create.
                format: int32
                type: integer
              minRunners:
                description: MinRunners is the minimum number of idle ephemeral runners
                  to keep.
                format: int32
                type: integer
              orgName:
                description: OrgName is the Gitea organization name (for org-scoped
                  runners).
                type: string
              runnerScope:
                description: 'RunnerScope is the scope of the runner: org or instance.'
                enum:
                - org
                - instance
                type: string
              template:
                description: |-
                  Template is the pod template spec for the runner pods.
                  A full corev1.PodTemplateSpec; its structure is preserved as-is rather
                  than fully schema-validated (embedded core types cannot be expressed as
                  a structural CRD schema without $ref, which the API server rejects).
                type: object
                x-kubernetes-preserve-unknown-fields: true
            required:
            - giteaConfigSecretRef
            - giteaConfigUrl
            - labels
            - orgName
            - runnerScope
            type: object
          status:
            description: GiteaRunnerSetStatus defines the observed state of GiteaRunnerSet.
            properties:
              availableReplicas:
                description: AvailableReplicas is the number of available runner pods.
                format: int32
                type: integer
              readyReplicas:
                description: ReadyReplicas is the number of ready runner pods.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
